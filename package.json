{
  "name": "learnify-frontend",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "python -m uvicorn backend.main:app --reload --port 8000",
    "start": "python -m uvicorn backend.main:app --port 8000",
    "fe": "npx serve public",
    "seed:db": "python -m backend.seed_supabase"
  }
}
